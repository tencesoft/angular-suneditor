import { InjectionToken } from '@angular/core';

/**
 * Injection token for options object
 */
export const SUNEDITOR_OPTIONS = new InjectionToken('SUNEDITOR_OPTIONS');

/**
 * Feature Key used internal for editors injected as singleton
 */
export const ROOT_INJECTOR_KEY = 'root_injector_key';
